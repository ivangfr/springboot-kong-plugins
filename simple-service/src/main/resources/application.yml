spring:
  application:
    name: simple-service

management:
  security:
    enabled: false
  endpoints:
    web:
      exposure.include: health, httptrace
  endpoint:
    health:
      show-details: always